# Keyboard layout notes

`k295layout.txt` contains the keyboard matrix, lists of keys with their matrix coordinates, and a log of recorded key presses.

The data was collected by manually shorting the contacts on the PCB while the keyboard matrix was disconnected, to support adapting the keyboard for use with a different controller.
